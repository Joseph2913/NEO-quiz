# Dependencies
node_modules/

# Auth state (contains session tokens - never commit)
auth-state.json
auth-state-pa.json

# Runtime/output files (tenant-specific state, don't commit)
screenshots/
processing-log.json
quiz-runtime-state.json
pipeline-results.json
progress.json
results.json
diagnostics/
form-editor-dump.html
manifest.json

# Claude Code local config
.claude/

# OS files
.DS_Store
Thumbs.db

# Source documents (large binaries, not needed for the tool)
*.pptx
*.xlsx
*.pdf
*.png

# Editor
.vscode/
.idea/
